<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.mymrceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".pdfviewer" />
        <activity
            android:name=".bus_routes"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".dummy_1" />
        <activity android:name=".contact" />
        <activity android:name=".canteen" />
        <activity android:name=".notification" />
        <activity android:name=".cells" />
        <activity android:name=".student_chapters" />
        <activity android:name=".events" />
        <activity android:name=".faculty" />
        <activity android:name=".notice" /> <!-- <activity android:name=".bus_routes" /> -->
        <activity android:name=".cources" />
        <activity android:name=".gallery" />
        <activity android:name=".syllabus" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".firebasenotif">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>